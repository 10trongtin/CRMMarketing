name: crm_marketing
description: App quản lý tiến độ công việc phòng Marketing
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.1
  intl: ^0.20.2
  google_fonts: ^8.2.0
  flutter_slidable: ^4.0.3
  cupertino_icons: ^1.0.6
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
